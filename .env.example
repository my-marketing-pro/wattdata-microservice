# ==========================================
# WATT DATA MCP SERVER CONFIGURATION
# ==========================================

# Your Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# Claude model to use
# Run `node check-models.js` to see which models are available on your account
CLAUDE_MODEL=claude-sonnet-4-5

# Transport type (use streamable for remote Watt Data server - recommended)
MCP_TRANSPORT_TYPE=streamable

# Authentication type (Watt Data uses Basic authentication)
MCP_AUTH_TYPE=basic

# Watt Data MCP Server URL
MCP_SERVER_URL=https://api.wattdata.ai/mcp

# API Key for Watt Data
# This should be base64(tokenId:tokenSecret)
#
# To generate your API key:
#   1. Log into Clerk web UI
#   2. Create a Machine for your customer (e.g., "Exact Match")
#   3. Within the Machine, create a new token
#   4. Record the Token ID and Token Secret
#   5. Encode: echo -n "tokenId:tokenSecret" | base64
#   6. Paste the result below
#
# Example:
#   Token ID: tok_123abc
#   Token Secret: sec_456def
#   Command: echo -n "tok_123abc:sec_456def" | base64
#   Result: dG9rXzEyM2FiYzpzZWNfNDU2ZGVm
#
MCP_SERVER_API_KEY=your_base64_encoded_token_here

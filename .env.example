# ==========================================
# Anthropic API Configuration
# ==========================================
# Your Anthropic API key (required for all configurations)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ==========================================
# MCP Transport Configuration
# ==========================================
# Transport type: 'sse' for remote servers (like Watt Data), 'stdio' for local servers
MCP_TRANSPORT_TYPE=sse

# Authentication type: 'basic' for Basic auth (Watt Data), 'bearer' for Bearer tokens, 'custom' for X-API-Key header
MCP_AUTH_TYPE=basic

# ==========================================
# WATT DATA MCP SERVER CONFIGURATION
# ==========================================
# Remote MCP server URL (for SSE transport)
MCP_SERVER_URL=https://your-watt-data-server.com/mcp

# Watt Data uses Basic Authentication with Machine-to-Machine tokens
# Format: base64(tokenId:tokenSecret)
# To get your token:
#   1. Create a Machine in Clerk web UI for your customer
#   2. Create a new token within the Machine
#   3. Record the Token ID and Token Secret
#   4. Encode as: echo -n "tokenId:tokenSecret" | base64
MCP_SERVER_API_KEY=your_base64_encoded_token_here

# Set this to true if the server expects 'X-API-Key' header instead of 'Authorization: Bearer'
# For Watt Data, leave this unset (it uses Authorization: Basic)
# MCP_SERVER_CUSTOM_HEADER=false

# ==========================================
# ALTERNATIVE: LOCAL MCP SERVER (STDIO)
# ==========================================
# Uncomment and set these to use a local MCP server instead

# MCP_TRANSPORT_TYPE=stdio
# MCP_SERVER_COMMAND=npx
# MCP_SERVER_ARGS=-y @modelcontextprotocol/server-everything

# ==========================================
# EXAMPLE CONFIGURATIONS
# ==========================================

# Example 1: Watt Data MCP Server
# MCP_TRANSPORT_TYPE=sse
# MCP_SERVER_URL=https://api.wattdata.com/mcp
# MCP_SERVER_API_KEY=dG9rZW5JZDp0b2tlblNlY3JldA==

# Example 2: Custom Remote MCP Server with Bearer token
# MCP_TRANSPORT_TYPE=sse
# MCP_AUTH_TYPE=bearer
# MCP_SERVER_URL=https://your-custom-server.com/mcp
# MCP_SERVER_API_KEY=your_bearer_token_here

# Example 3: Local MCP Server
# MCP_TRANSPORT_TYPE=stdio
# MCP_SERVER_COMMAND=node
# MCP_SERVER_ARGS=./path/to/your/mcp-server.js
